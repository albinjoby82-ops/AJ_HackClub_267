.claude/settings.local.json

# holds an API key — copy config.example.js instead
config.js

# Cloudflare local state and development secrets
.wrangler*/
.dev.vars*
.env*

# robot simulator build output
firmware/sim/build/
