// The Build Hub needs no client-side secrets.
// YouTube search uses the YOUTUBE_API_KEY environment variable read by
// tools/dev_server.py. Never place that key in this browser-facing file.

window.SITE_CONFIG = {};
